// querySelectorAll retorna todos los elementos que coincidan con la busqueda
const card = document.querySelectorAll('.card');
console.log(card);

// Si los elementos no existen
const noExiste = document.querySelectorAll('noExiste');
console.log(noExiste); // []